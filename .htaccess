<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Si el archivo existe, no reescribir
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Todo lo demás va a index.html
  RewriteRule ^ index.html [L]
</IfModule>